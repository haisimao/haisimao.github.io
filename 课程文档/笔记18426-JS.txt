JS:
   1)ECMAScript - 语法规范(ES标准)
   2)BOM - 浏览器对象模型 - window(负责浏览器操作)
   3)DOM - 文档对象模型 - document(负责页面操作,所有的标签都是这个树下的根)

   一般层叠样式表前置,js后置
   
   python靠':'与缩进来控制语句块
   其他语言大多用{}来控制语句块

   <!--表示直接把"js/js11.js",写到script标签的中间-->
   <script src="js/js11.js"></script>

   Math.random() 产生一个[0~1)的随机数


   得到getElementById()可以获得一个标签的对象,可以用这个对象访问所有的标签属性(写在标签内部的都是)
    
    this 代表当前对象



    HTML标签可以看做是一个对象,CSS操作这些对象的属性渲染页面,JS用来操作这些对象,和事件做事情

   






































