安装软件:包管理工具->yum rpm(.src格式)->构建安装 -> .bin格式(解压缩已安好的软件,只需要配置相应文件)->修改环境变量(export PATH=$PATH:目标软件的bin目录) (但只是临时)/进入.bash_profile(修改环境变量)

rpm -i/-e 程序名 --红帽子 -i(安装) -e(移除) -qa(查询安装过的软件)  安装.src格式程序文件
xargs    rpm -qa | grep jdk | xargs rpm -e (xargs 命令,将前面管道过滤的结果作为参数传给下面命令)

yum -> 包管理工具:
yum install gcc --包管理工具 -- 安装其他所有软件先安装gcc
yum install/remove|list installed|search|makecache(创建目录缓存)软件名yum list installed | grep Nginx --查看是否安装某个程序
当安装程序时提示缺少依赖库依赖库(安装时报错 ->** is needed)解决方案 -> yum search -->yum install

构建安装: 应该是所有的文件
        解压下载文件 -> 进入解压的安装文件 -> 运行该文件夹下的配置文              件./config***-> 再make && make install

pip install 软件名

安装具体文件注意事项:
redis.conf数据库 -- 修改配置文件(先备份) -- 主要修改配置文件的bind/port
