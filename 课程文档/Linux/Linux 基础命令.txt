IP地址：
(公) 120.79.255.34
(内) 172.16.252.77
密码:199301Ac~
root管理员登录密码--199301Ac~
939774qml.

centOS7.4
操作系统进行的程序控制:
    操作系统中主要功能是包括：进程管理、存储管理、设备管理、文件管理和作业管理的工作。
    进程（Process）是系统中正在运行的程序实例。
    作业是系统为完成一个用户的计算任务（或一次事务处理）所做的工作总和。例如，对于用户编制的源程序，需经过对源程序的编译、连接编辑或连接装入及运行产生计算结果。这其中的每一个步骤，常称为作业步，作业步的顺序执行即完成了一个作业。





通用操作系统(没有与任何硬件绑定)--Linux -- 云
服务
发行版本-REDhat Linux, Ubuntu linux,CentOS linux

#(超级管理员
$(普通用户)

专用操作系统(与硬件绑定)

命令:他们有很多参数,查看可以通过--man 命令 ,参数之间还可以组合

格式:命令 命令参数 命令对象

系统上所有的设备都可以找到一个跟它对应的文件

who,whoami who am i --查看自己是谁
 
logout exit -退出服务器

shutdown--关闭服务器

reboot -- 重启

init 0--关机

init 6 -- 重启

ps -- 查看自己使用的shell (人和计算机对话的页面)

w -- 查看自己是谁

uname -- 查看系统类型

adduser -- 创建用户名

passwd 用户名 -- 修改密码

whereis 文件/命令 --查看命令的位置

which 文件/命令 --查看命令的位置

命令 --help | less/more --查看命令的位置

history --查找到历史命令,!+命令行号(!num)+enter再次执行该命令


重要命令:

su 用户名--切换用户

sudo --以管理员的身份执行

grep 正则表达式或者其他字符串--搜索文件内容--cat python3-tutorial.html |      grep \<div .*\>------cat python3-tutorial.html | grep '<div .*>'
     grep \<div .*\> 文件路径(python3-tutorial.html) --- 
     grep '<div .*>' 文件路径(python3tutorial.html) -n(显示行号) -R(显       示路径) &(把命令转入后台执行)
     cat python3-tutorial.html | grep '<div .*>' / -n -R >result.txt        2>error.txt & --在根目录下查找文件显示行,显示路径,将搜索结果存入指定      文件中

Ctrl + C 终止一个命令的执行
Ctrl + Z 暂停一个命令的执行

> 文件名--输出重定向,当再次重定向到同一文件时，会覆盖之前的内容
2>文件名--将输出时出错的内容放入到指定文件夹
>> 文件名--追加输出重定向,以追加的方式,输出重定向,将输出时正确的文件追加到到指定文件中
< 文件名---输入重定向

jobs --查看当前后台运行的所有任务

fg %num-- 把num号任务放到前台执行

bg %num-- 把num号任务放到后台执行

date/cal(查看日历)

echo 内容 >名 --回声命令,能将内容写入文件中--echo $PATH产看环境变量

wall 内容--发警告给所有人

alias 命令别名='命令 -参数'  ---给命令取别名--unalias 别名--给命令反别名

cal 月 年 --得到一个月的日历

在vim 的命令模式下搜索替换
:n1,n2s/word1/word2/g[i,[e,[c],g -Globle,i -(忽略大小写) e -(忽略大小写在命令模式下输入 
! 命令 --可以执行系统命令
yy -copy ,p --粘贴,dd删除,set nu--设置行号

ping 域名 :查看是否能连通 












