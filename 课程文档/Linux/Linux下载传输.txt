格式:命令 命令参数 命令对象

ps -a -e -f:查看所有进程,显示环境变量,显示进程间的关系

kill -9 进程号(PID):强制终止进程

netstat 命令用于显示各种网络相关信息，如网络连接，路由表，接口状态等等
netstat -a --列出所有端口 (包括监听和未监听的)IP
netstat -at --列出所有 tcp 端口
netstat -nap 查看端口号是否被占用

host domain  DNS查询，寻找域名domain对应的IP:
host IP   反向DNS查询:

文件上传:
sftp ,scp连接上某个服务器下载东西
把你的文件打包到百度云盘,然后用里面的连接wget url获得资源
Xftp --登上服务器用图形化界面上传文件

wget url
常用选项:–limit-rate :下载限速
         -o：指定日志文件；输出都写入日志,对日志进行操作
         -c：断点续传

sftp文件传输:sftp ID@IP
登陆服务器host，ID为用户名。sftp登陆后，可以使用下面的命令进一步操作：
        get -r filename # 下载文件
        put -r filename # 上传文件
        ls # 列出host上当前路径的所有文件
        cd # 在host上更改当前路径
        lls # 列出本地主机上当前路径的所有文件
        lcd # 在本地主机更改当前路径

scp localpath ID@host:path   将本地localpath指向的文件上传到远程主机的path路径:
scp -r ID@site:path localpath  以ssh协议，遍历下载path路径下的整个文件系统，到本地的localpath:
复制文件:

scp local_file remote_username@remote_ip:remote_folder
scp local_file remote_username@remote_ip:remote_file
scp local_file remote_ip:remote_folder
scp local_file remote_ip:remote_file
指定了用户名，命令执行后需要输入用户密码；如果不指定用户名，命令执行后需要输入用户名和密码；

复制目录:

scp -r local_folder remote_username@remote_ip:remote_folder
scp -r local_folder remote_ip:remote_folder
第1个指定了用户名，命令执行后需要输入用户密码； 第2个没有指定用户名，命令执行后需要输入用户名和密码；


上传本地文件到远程机器指定目录
scp /opt/soft/demo.tar root@10.6.159.147:/opt/soft/scptest

缺少依赖库,,,,yum search --yum install 依赖库(安装时报错的** is needed)
ping 域名 :查看是否能连通  --daemom 守护进程(跟随系统的启动与停止)

注意，一定要写成 ./test.sh，而不是 test.sh，运行其它二进制的程序也一样，直接写 test.sh，linux 系统会去 PATH 里寻找有没有叫 test.sh 的，而只有 /bin, /sbin, /usr/bin，/usr/sbin 等在 PATH 里，你的当前目录通常不在 PATH 里，所以写成 test.sh 是会找不到命令的，要用 ./test.sh 告诉系统说，就在当前目录找。

pip3 install iPython
安装Jupyter

负载均衡,双活(doubleliveing)

分布式拒绝服务攻击--DDoS高防IP:发很多无意义的数据,让你的服务器瘫痪































