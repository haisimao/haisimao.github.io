编程范式(理念)-面向对象编程/函数式编程

面向对象编程:用对象做事情,就是要调对象的方法,该对象的属性

ipython

弱类型语言 -变量类型有赋值的对象决定

类之间的关联关系:

编写函数,先把问题翻译成自然语言,然后将自然语言编写成函数

尽量减少使用全局变量

github.com

coding.net

oschina.net

gitee.com*******************
https://gitee.com/HaiSiMiao/events

字符串等序列截取[a:b :c] (c决定了截取的方向,和a,b的相对位置)从a开始,按照c的步长截取     |b|-|a|个元素.c为负数时,序列最右边的下标是-1.

在构造函数时,有些函数是回调函数,有些是委托函数,这些函数的调用地点和时间不确定,由浏览器或者编译器,解释器为他们传递参数

python中调用系统命令--os.system('命令')

echo 相当于print

变量在栈上,对象在堆上

非关系型数据库redis

vim 的配置文件放在.vimrc这个隐藏文件中

#!/usr/bin/python   ----在文件中添加让python程序可以自动执行
#encoding:utf-8     ----指定编码

环境变量.在这里面的程序不管在哪里都能被执行

yum 安装软件(类似于360安装助手)

用安装源文件去安装软件,

Nginx -->Web服务器

MySQL -->关系型数据库 - 数据持久化

Redis -->非关系型数据库 - 数据持久化

防火墙 - iptables / firewall 防火墙有网络防火墙和计算机防火墙的提法。网络防火墙是指在外部网络和内部网络之间设置网络防火墙。这种防火墙又称筛选路由器。网络防火墙检测进入信息的协议、目的地址、端口（网络层）及被传输的信息形式（应用层）等，滤除不符合规定的外来信息。

max *mark min = [1,2,3,4,5,6,7] --list解序 取出中间的给Mark

from collections import Counter //序列操作的模块,做统计用
counter = Counter(序列);counter.most_common(参数)

pip3 install ipython  //%timeit (1,2,3,4) 查看一个数据建立需要的时间

pip install jupyter  // jupyter notebook 可以在浏览器打开一个笔记本,可以写代码做笔记

import sys // sys.argv收集的是命令行参数(是一个列表,这个列表包括命令本) // 用来在执行程序时,可以在程序后面添加命令行参数,,这个命令行参数会传入程序,然后就可以在程序中使用这些参数

nonlocal 把一个函数里面的函数里面的变量作用域,添加到嵌套作用域
generator object 生成器对象   可以遍历 list1 = (x for i in range(1,10))
    list2 = [x for i in range(1,10)] list1 是生成器对象,list2是列表对象

IBM开发者社区

DNS服务器 - 域名翻译成IP地址

HTTP服务器 - Apache
     LNMP = Linux + Nginx + MySQL + Python
             Nginx:反向代理:相当于中间服务器,可以帮你找到最合适真正服务器

文件操作:压缩,解压,grep,访问,创建,删除,打开,快捷方式,vim,上传(sftp)

下载传输:wget,scp,sftp,ps,netstat

安装软件:包管理工具->yum rpm,.src格式->构建安装,.bin格式(解压缩已安好的软件,只需要配置相应文件)->修改环境变量(export PATH=$PATH:目标软件的bin目录) (但只是临时)/进入.bash_profile(修改环境变量)

文件访问与运行:ln -s, vim, cat, alias 

shell脚本:脚本名.sh->修改文件权限就可执行  -- ./test.sh

时间和空间是不可调和的矛盾   **** 缓存
硬件和软件在逻辑上是等效的   ****

在linux环境下，任何事物都以文件的形式存在，通过文件不仅仅可以访问常规数据，还可以访问网络连接和硬件。

shell是操作系统的人机交互接口,是操作系统的一部分

操作系统的输入井,输出井--缓冲区,操作系统进行IO操作时,会起一个中断,(操作系统不会允许这样的情况),所以操作系统会将输出走缓冲区,即操作系统会将输出输入到输出井内,等输出达到最大值,才输出.

jekyll,hexo ,--markdown 
./configure

















 