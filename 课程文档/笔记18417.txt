设计函数时如果函数有返回值要保证每一条执行路径都有返回值而且返回值的类型是一致的

如果想要在循环里面结束循环,,除了break,,还可以用标志位...flage = True while flage;  pass

fastcapture

# ctrl + P 可以查看函数参数(实时)
# ctrl + 值 可以查看值底层定义

多台独立自主的计算机,互联而成,组成的网络,就是计算机网络

在交互式下可以用工具''2 to 3 ''将python2.x的代码转换换为python3.x的代码

解决问题:把功能函数化,把同样的对象类化
         建立类:找到类的成员属性,行为属性
	 类建立出来了,,不要想着一步登天,问题           要一步一步的解决,不要想要只用一个类
	 或者一个函数解决掉所有问题.
