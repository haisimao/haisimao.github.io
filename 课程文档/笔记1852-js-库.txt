BOM:浏览器对象模型(window)

CSS抠图:用像素偏移到要扣的图的其实位置,然后用放图的大小来最后抠出想要的图

认真理解昨天的作业,从新来一遍

addEventListener的方法的第三个参数表示事件处理方式(默认false),事件传播的行为与标签是否绑定事件无关
true-事件冒泡:从内向外传播事件
false-事件捕获:从外向里传播事件

阻止事件的传播:evt.stopPropagation()
              evt.cancelBubble = true(IE)

this:当前对象,谁调用了就是谁
